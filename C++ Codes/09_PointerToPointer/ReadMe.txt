-we can assign pointer to pointer means lets consider example i is int variable and p is pointer holding address of i ,lets say we define one more pointer q 
which holds address of p means q is pointer to p (Pointer to Pointer)
	eg. int i=25;
 	    int *p=&i;
	    int **q=&p;
