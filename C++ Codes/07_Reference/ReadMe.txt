A reference is defines an alternative name for an object( Alis name).
ex:
int a=10;
int &Ref_a=a; //&Ref_a is a reference for as

we can't keep reference un-initialize for ex:

int &Ref_b; //error: A reference must be initialized